[package]
name = "builder"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
raiko-pipeline = { workspace = true }


[features]
sp1 = ["raiko-pipeline/sp1"]
risc0 = ["raiko-pipeline/risc0"]

